package com.example.nurture_nest.com.example.nurture_nest

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.nurture_nest.R

class TeacherDashboard : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.fragment_teacher_dashboard) // Use your new activity layout

        // Initialize your views here like buttons, ImageViews, etc.
    }
}